export enum UgcPostType {
  VIDEO = "VIDEO",
  IMAGE = "IMAGE",
}

export enum PostStatus {
  PENDING_REVIEW = "pending_review",
  PUBLISHED = "published",
  REJECTED = "rejected",
  ARCHIVED = "archived",
}
